spring.data.rest.base-path=/api
server.port=8081

# ENDPOINTS CORS CONFIGURATION (EndpointCorsProperties)
# this doesn't work yet - https://jira.spring.io/browse/DATAREST-573

#endpoints.cors.allow-credentials=true # Set whether credentials are supported. When not set, credentials are not supported.
#endpoints.cors.allowed-headers=* # Comma-separated list of headers to allow in a request. '*' allows all headers.
#endpoints.cors.allowed-methods=* # Comma-separated list of methods to allow. '*' allows all methods.
#endpoints.cors.allowed-origins=http://localhost:3000 # Comma-separated list of origins to allow. '*' allows all origins. When not set, CORS support is disabled.
#endpoints.cors.exposed-headers= # Comma-separated list of headers to include in a response.
#endpoints.cors.max-age=1800 # How long, in seconds, the response from a pre-flight request can be cached by clients.

